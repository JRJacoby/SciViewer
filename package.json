{
  "name": "sci-viewer",
  "displayName": "SciViewer",
  "description": "View scientific data files (HDF5, pickle, etc.) in VS Code",
  "version": "0.2.0",
  "publisher": "JohnJacoby",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/JRJacoby/SciViewer"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Data Science",
    "Other"
  ],
  "keywords": [
    "hdf5",
    "h5",
    "pickle",
    "pkl",
    "parquet",
    "pqt",
    "scientific",
    "data",
    "viewer",
    "numpy",
    "hierarchical"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "sciViewer.h5",
        "displayName": "HDF5 Viewer",
        "selector": [
          { "filenamePattern": "*.h5" },
          { "filenamePattern": "*.hdf5" }
        ],
        "priority": "default"
      },
      {
        "viewType": "sciViewer.pickle",
        "displayName": "Pickle Viewer",
        "selector": [
          { "filenamePattern": "*.pkl" },
          { "filenamePattern": "*.pickle" },
          { "filenamePattern": "*.p" }
        ],
        "priority": "default"
      },
      {
        "viewType": "sciViewer.parquet",
        "displayName": "Parquet Viewer",
        "selector": [
          { "filenamePattern": "*.parquet" },
          { "filenamePattern": "*.pqt" }
        ],
        "priority": "default"
      }
    ],
    "configuration": {
      "title": "SciViewer",
      "properties": {
        "sciViewer.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Path to Python interpreter. Leave empty to auto-detect (python3 on Unix, python on Windows)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
